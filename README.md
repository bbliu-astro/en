# Local mirror of Beibei Liu's Google Sites homepage

Open `index.html` directly, or run a local web server:

```bash
cd site
python3 -m http.server 8000
```

Then visit http://localhost:8000/.

This is a static snapshot generated from the public Google Sites pages. External scholarly links, mailto links, ADS/arXiv links, and WeChat links remain external.
